module.exports (terms) = terms.term {
    constructor () =
        self.is break = true

    generate statement (scope) =
        self.code 'break;'

    rewrite result term into (return term) = self
}
