module.exports (terms) = terms.term {
    constructor () =
        self.is callback = true
    
    parameter () =
        self

    generate (scope) =
      terms.callbackFunction.generate (scope)
}
